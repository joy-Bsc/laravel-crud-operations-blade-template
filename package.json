{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'No build needed - using plain Blade SPA'",
        "dev": "echo 'No dev server needed - using plain Blade SPA'"
    },
    "devDependencies": {},
    "dependencies": {},
    "optionalDependencies": {}
}

